import styled from "styled-components";

export const CharacteristicsList = styled.dl`
  padding-top: 4px;
  margin: 0;
  overflow: hidden;
`;

export const CharacteristicsItem = styled.dt`
  box-sizing: border-box;
  font-weight: 700;
  margin: 0;
  float: left;
  clear: both;
`;

export const CharacteristicsValue = styled.dd`
  margin: 0;
  display: inline;
  padding-left: 5px;
  box-sizing: border-box;
  font-weight: 400;
`;
