import styled from "styled-components";

export const PropertiesList = styled.dl`
  overflow: hidden;
  margin: 0;
  padding-top: 4px;
`;

export const PropertiesItem = styled.dt`
  box-sizing: border-box;
  font-weight: 700;
  margin: 0;
  float: left;
  clear: both;
`;

export const PropertiesValue = styled.dd`
  margin: 0;
  display: inline;
  padding-left: 5px;
  box-sizing: border-box;
  font-weight: 400;
`;
